// a base class that defines all entities in the game
import {DisplaySymbol} from './display_symbol.js';


class Entity extends DisplaySymbol{

  constructor(entityData){
    super(entityData);
  }

}
